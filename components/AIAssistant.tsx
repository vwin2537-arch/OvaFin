import React from 'react';
import type { Transaction } from '../types';

// Component disabled as per user request and service removal
export const AIAssistant: React.FC<{ transactions: Transaction[] }> = () => {
    return null;
};